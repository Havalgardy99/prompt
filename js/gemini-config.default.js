/**
 * Default Gemini config (no API key). Used on production so the script loads without 404.
 * For local dev with a key: copy gemini-config.sample.js to gemini-config.js and set your key
 * (keep js/gemini-config.js in .gitignore).
 */
window.GEMINI_API_KEY = '';
window.GEMINI_MODEL = '';
